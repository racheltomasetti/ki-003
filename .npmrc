# React Native and its ecosystem expect packages to be hoisted to the root
# node_modules like npm does. Without this, CocoaPods and Metro can't resolve
# packages through pnpm's virtual store structure.
shamefully-hoist=true
